# structured-LLM-engine
AI system that converts natural language prompts into structured spatial layouts. It validates JSON outputs, enforces constraints like non-overlapping rooms, and visualises results in 2D. Includes self-correcting retry logic to ensure reliable, production-ready outputs from LLM-generated data.
